# Apps

The canonical runtime UI lives in `core/static/` and is served by `nexora-core` for web/mobile/smartwatch responsive access.

This folder documents platform packaging targets:

- `apps/web`: web distribution notes.
- `apps/mobile`: PWA wrapper notes (Android/iOS).
- `apps/watch`: smartwatch compact UI mapping.
